<%= react_component 'RunSessionFilterPage', {tag_path: request.url, questions_path: cards_run_filter_path, number_questions: @number_sessions, total: @number_sessions.to_i, run_cards_path: run_sessions_path(session_id: @id_session.to_i)}%>
