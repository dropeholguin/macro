<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Welcome to Macro-SAS, <%= @question.user.name %></h1>
    <p>
      Your question was suspended
    </p>
    <p>
      <% if @question.flags.empty? %>
        Flag Comments: 
        <% @question.flags.each do |flag| %>
          <tr>
            <td>- <%= markdown(flag.reason) %></td>
          </tr>
        <% end %>
      <% end %>
    </p>
  </body>
</html>