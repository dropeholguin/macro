<% if user_signed_in? %>      
<% else %>  
  <%= react_component 'Home', {csrfToken: form_authenticity_token, linkedin: user_linkedin_omniauth_authorize_path, arrow_img: image_path('arrow.png'), card_img: image_path('cards.png'), session_img: image_path('session.png'), submit_img: image_path('submit.png'), stock_img: image_path('stock-2bg.jpg', user: @user.points)} %>    
<%end%>  
