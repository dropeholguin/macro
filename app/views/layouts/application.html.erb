<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= content_for?(:title) ? yield(:title) : "Macro-people" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <% if user_signed_in? %>      
      <%= react_component 'HeaderLoggedIn', {user: current_user.email, sign_out: destroy_user_session_path,  points: current_user.points} %>
    <% else %>  
    <%end%>    
    <%= yield %>
  </body>
</html>
